<section class="page-404" aria-labelledby="page-404-heading">
  <div class="container page-404__inner">

    <!-- Large decorative 404 -->
    <div class="page-404__number" aria-hidden="true">404</div>

    <!-- Logo mark -->
    <div class="page-404__icon" aria-hidden="true">
      <svg width="56" height="56" viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
        <circle cx="16" cy="16" r="14" fill="#00d4a8" opacity="0.15"/>
        <circle cx="16" cy="16" r="8" fill="#00d4a8"/>
        <path d="M16 8 Q20 12 16 16 Q12 20 16 24" stroke="#0f1f3a" stroke-width="2" fill="none"/>
      </svg>
    </div>

    <h1 class="page-404__heading" id="page-404-heading">Page not found</h1>
    <p class="page-404__body">
      Sorry, the page you were looking for doesn't exist or may have moved.<br/>
      Let us help you find what you need.
    </p>

    <!-- Search form -->
    <form class="page-404__search" action="{{ routes.search_url }}" method="get" role="search" aria-label="Site search">
      <input type="hidden" name="type" value="product,article,page" />
      <div class="page-404__search-wrap">
        <input
          type="search"
          name="q"
          class="page-404__search-input"
          placeholder="Search products, treatments…"
          aria-label="Search"
          autocomplete="off"
        />
        <button type="submit" class="page-404__search-btn" aria-label="Submit search">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
            <circle cx="11" cy="11" r="8"/>
            <line x1="21" y1="21" x2="16.65" y2="16.65"/>
          </svg>
        </button>
      </div>
    </form>

    <!-- Quick links -->
    <nav class="page-404__links" aria-label="Helpful links">
      <a href="{{ routes.root_url }}" class="btn btn-primary btn-lg">Go to Homepage</a>
      <a href="{{ routes.collections_url }}" class="btn btn-outline btn-lg">Shop Products</a>
    </nav>

    <!-- Popular pages -->
    <div class="page-404__popular">
      <p class="page-404__popular-label">Popular pages</p>
      <ul>
        <li><a href="{{ routes.root_url }}#why-us">Why LogixX</a></li>
        <li><a href="{{ routes.root_url }}#products">Our Products</a></li>
        <li><a href="{{ routes.root_url }}#faq">FAQs</a></li>
        <li><a href="{{ routes.root_url }}#email-cta">Free Consultation</a></li>
      </ul>
    </div>

  </div>
</section>

<style>
/* ---- 404 page styles ---- */
.page-404 {
  min-height: calc(100vh - var(--header-height) - 60px);
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--grey-50);
  padding: var(--sp-11) var(--sp-3);
}

.page-404__inner {
  text-align: center;
  max-width: 560px;
}

.page-404__number {
  font-family: var(--font-serif);
  font-size: clamp(6rem, 18vw, 10rem);
  font-weight: 400;
  line-height: 1;
  color: var(--navy-800);
  opacity: 0.08;
  letter-spacing: -4px;
  margin-bottom: -1.5rem;
  user-select: none;
}

.page-404__icon {
  display: flex;
  justify-content: center;
  margin-bottom: var(--sp-5);
}

.page-404__heading {
  font-family: var(--font-serif);
  font-size: clamp(1.8rem, 4vw, 2.6rem);
  color: var(--navy-800);
  margin-bottom: var(--sp-3);
}

.page-404__body {
  font-family: var(--font-sans);
  font-size: 1.05rem;
  color: var(--grey-500);
  line-height: 1.7;
  margin-bottom: var(--sp-7);
}

/* Search */
.page-404__search {
  margin-bottom: var(--sp-7);
}

.page-404__search-wrap {
  display: flex;
  border: 1.5px solid var(--grey-200);
  border-radius: var(--radius-pill);
  overflow: hidden;
  background: #fff;
  box-shadow: var(--shadow-sm);
  transition: box-shadow var(--transition-fast), border-color var(--transition-fast);
}

.page-404__search-wrap:focus-within {
  border-color: var(--mint-500);
  box-shadow: 0 0 0 3px rgba(0, 212, 168, .18);
}

.page-404__search-input {
  flex: 1;
  border: none;
  outline: none;
  padding: 14px 20px;
  font-family: var(--font-sans);
  font-size: 15px;
  color: var(--navy-800);
  background: transparent;
}

.page-404__search-input::placeholder {
  color: var(--grey-400);
}

.page-404__search-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 52px;
  background: var(--mint-500);
  border: none;
  color: #fff;
  cursor: pointer;
  transition: background var(--transition-fast);
  flex-shrink: 0;
}

.page-404__search-btn:hover {
  background: var(--mint-600);
}

/* CTA links */
.page-404__links {
  display: flex;
  gap: var(--sp-3);
  justify-content: center;
  flex-wrap: wrap;
  margin-bottom: var(--sp-7);
}

/* Popular pages */
.page-404__popular-label {
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 600;
  letter-spacing: .08em;
  text-transform: uppercase;
  color: var(--grey-400);
  margin-bottom: var(--sp-2);
}

.page-404__popular ul {
  list-style: none;
  display: flex;
  gap: var(--sp-4);
  justify-content: center;
  flex-wrap: wrap;
}

.page-404__popular a {
  font-family: var(--font-sans);
  font-size: 14px;
  color: var(--blue-500);
  text-decoration: none;
  transition: color var(--transition-fast);
}

.page-404__popular a:hover {
  color: var(--mint-600);
  text-decoration: underline;
}
</style>
